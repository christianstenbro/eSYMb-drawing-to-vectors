# eSYMb-drawing-to-vectors
The repository contains tools to extract vectors from eSYMb line drawings

To run the script, please set-up a new Python venv (with Python version = 3.13.2) and install the packages from requirements.txt.

## Data
The raw image files from the different studies can be stored in study-specific subfolders in the drawing_samples folder.
